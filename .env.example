# Neon Postgres connection string
DATABASE_URL="postgresql://user:password@ep-xxxx.neon.tech/rheewaz?sslmode=require"

# Sign session cookies. Generate with: openssl rand -base64 32
SESSION_SECRET="replace-with-a-long-random-secret"

# Bootstrap accounts (seed script only)
SEED_BOSS_EMAIL="bellowk55@gmail.com"
SEED_BOSS_PASSWORD="changeyourpassword"
SEED_ADMIN_EMAIL="bystus289@gmail.com"
SEED_ADMIN_PASSWORD="changeyourpassword"

# Known worker seed accounts + truck assignments (Phase 2)
SEED_WORKER1_NAME="Worker One"
SEED_WORKER1_EMAIL="yb3947209@gmail.com"
SEED_WORKER1_PASSWORD="changeyourpassword"
SEED_WORKER1_TRUCK="Daihatsu Hijet"

SEED_WORKER2_NAME="Worker Two"
SEED_WORKER2_EMAIL="wokorhenrybabatunde@gmail.com"
SEED_WORKER2_PASSWORD="changeyourpassword"
SEED_WORKER2_TRUCK="Toyota Dyna"

# Authorizes the weekly-rollover cron endpoint
CRON_SECRET="replace-with-another-long-random-secret"

# Email (optional — falls back to console-logging the verification link if unset)
RESEND_API_KEY=""
EMAIL_FROM="Rheewaz Waters <onboarding@resend.dev>"
APP_URL="http://localhost:3000"

# --- Phase 2: Live camera -------------------------------------------------
# HLS (.m3u8) URL for the Boss's live loading-dock feed. Leave unset to show
# an honest "CAMERA OFFLINE — not configured" state instead of fake video.
# This app never fabricates a video feed.
CAMERA_STREAM_URL=""
CAMERA_LABEL="Loading Dock"

# --- Phase 2: AI verification / training ----------------------------------
# This app does NOT run a real computer-vision model. It provides the data
# model, rules engine, and review/training workflow that a real inference
# service would plug into via AI_INFERENCE_WEBHOOK_SECRET below. Until you
# connect a real model, AI predictions are entered by the Boss/Admin as
# manual verification, clearly labeled predictionSource='manual'.
AI_INFERENCE_WEBHOOK_SECRET="replace-with-another-long-random-secret"
